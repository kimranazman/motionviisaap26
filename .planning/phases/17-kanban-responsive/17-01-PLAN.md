---
phase: 17-kanban-responsive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/kanban/kanban-board.tsx
  - src/components/pipeline/pipeline-board.tsx
  - src/components/potential-projects/potential-board.tsx
autonomous: true

must_haves:
  truths:
    - "User on mobile can swipe horizontally to see more columns"
    - "User on mobile can tap-and-hold a card to start dragging"
    - "Horizontal scrolling does not conflict with drag-and-drop"
  artifacts:
    - path: "src/components/kanban/kanban-board.tsx"
      provides: "Touch sensor configuration for initiatives"
      contains: "TouchSensor"
    - path: "src/components/pipeline/pipeline-board.tsx"
      provides: "Touch sensor configuration for pipeline"
      contains: "TouchSensor"
    - path: "src/components/potential-projects/potential-board.tsx"
      provides: "Touch sensor configuration for potentials"
      contains: "TouchSensor"
  key_links:
    - from: "All board components"
      to: "dnd-kit sensors"
      via: "MouseSensor + TouchSensor instead of PointerSensor"
      pattern: "useSensor\\(TouchSensor"
---

<objective>
Configure touch-friendly drag-and-drop and horizontal scroll containers for all 3 Kanban boards.

Purpose: Replace PointerSensor with MouseSensor + TouchSensor combo for reliable touch support. Add horizontal scroll with CSS snap for mobile column navigation.
Output: All 3 boards support touch drag (250ms delay) and horizontal swipe scrolling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-kanban-responsive/17-RESEARCH.md

@src/components/kanban/kanban-board.tsx
@src/components/pipeline/pipeline-board.tsx
@src/components/potential-projects/potential-board.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update KanbanBoard with touch sensors and scroll container</name>
  <files>src/components/kanban/kanban-board.tsx</files>
  <action>
Replace PointerSensor with MouseSensor + TouchSensor:

1. Update imports - add MouseSensor, TouchSensor from @dnd-kit/core

2. Replace sensors configuration:
```typescript
const mouseSensor = useSensor(MouseSensor, {
  activationConstraint: { distance: 5 },
})

const touchSensor = useSensor(TouchSensor, {
  activationConstraint: {
    delay: 250,     // 250ms hold before drag starts
    tolerance: 5,   // 5px movement allowed during delay
  },
})

const keyboardSensor = useSensor(KeyboardSensor, {
  coordinateGetter: sortableKeyboardCoordinates,
})

const sensors = useSensors(mouseSensor, touchSensor, keyboardSensor)
```

3. Update board container div (currently `flex gap-4 min-w-max pb-4`):
```typescript
<div className={cn(
  "flex gap-4 pb-4",
  // Mobile: horizontal scroll with snap
  "overflow-x-auto snap-x snap-mandatory overscroll-x-contain",
  // Desktop: standard min-width
  "md:min-w-max md:snap-none"
)}>
```

Note: Import cn from @/lib/utils if not already imported.
  </action>
  <verify>
- TypeScript compiles without errors: `npm run build`
- grep for "TouchSensor" in kanban-board.tsx shows import and usage
  </verify>
  <done>
KanbanBoard uses MouseSensor + TouchSensor with 250ms delay, container has horizontal scroll snap on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PipelineBoard with touch sensors and scroll container</name>
  <files>src/components/pipeline/pipeline-board.tsx</files>
  <action>
Apply identical touch sensor and scroll container changes to PipelineBoard:

1. Update imports - add MouseSensor, TouchSensor from @dnd-kit/core

2. Replace sensors configuration (same as Task 1):
```typescript
const mouseSensor = useSensor(MouseSensor, {
  activationConstraint: { distance: 5 },
})

const touchSensor = useSensor(TouchSensor, {
  activationConstraint: {
    delay: 250,
    tolerance: 5,
  },
})

const keyboardSensor = useSensor(KeyboardSensor, {
  coordinateGetter: sortableKeyboardCoordinates,
})

const sensors = useSensors(mouseSensor, touchSensor, keyboardSensor)
```

3. Update board container div (currently `flex gap-4 min-w-max pb-4 overflow-x-auto`):
```typescript
<div className={cn(
  "flex gap-4 pb-4",
  "overflow-x-auto snap-x snap-mandatory overscroll-x-contain",
  "md:min-w-max md:snap-none"
)}>
```

Import cn from @/lib/utils if not already imported.
  </action>
  <verify>
- TypeScript compiles without errors: `npm run build`
- grep for "TouchSensor" in pipeline-board.tsx shows import and usage
  </verify>
  <done>
PipelineBoard uses MouseSensor + TouchSensor with 250ms delay, container has horizontal scroll snap on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PotentialBoard with touch sensors and scroll container</name>
  <files>src/components/potential-projects/potential-board.tsx</files>
  <action>
Apply identical touch sensor and scroll container changes to PotentialBoard:

1. Update imports - add MouseSensor, TouchSensor from @dnd-kit/core

2. Replace sensors configuration (same as Tasks 1-2):
```typescript
const mouseSensor = useSensor(MouseSensor, {
  activationConstraint: { distance: 5 },
})

const touchSensor = useSensor(TouchSensor, {
  activationConstraint: {
    delay: 250,
    tolerance: 5,
  },
})

const keyboardSensor = useSensor(KeyboardSensor, {
  coordinateGetter: sortableKeyboardCoordinates,
})

const sensors = useSensors(mouseSensor, touchSensor, keyboardSensor)
```

3. Update board container div (currently `flex gap-4 min-w-max pb-4 overflow-x-auto`):
```typescript
<div className={cn(
  "flex gap-4 pb-4",
  "overflow-x-auto snap-x snap-mandatory overscroll-x-contain",
  "md:min-w-max md:snap-none"
)}>
```

Import cn from @/lib/utils if not already imported.
  </action>
  <verify>
- TypeScript compiles without errors: `npm run build`
- grep for "TouchSensor" in potential-board.tsx shows import and usage
  </verify>
  <done>
PotentialBoard uses MouseSensor + TouchSensor with 250ms delay, container has horizontal scroll snap on mobile.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build` completes without errors
2. All 3 boards have TouchSensor configured:
   - `grep -l "TouchSensor" src/components/kanban/kanban-board.tsx src/components/pipeline/pipeline-board.tsx src/components/potential-projects/potential-board.tsx`
3. All 3 boards have scroll snap classes:
   - `grep -l "snap-x snap-mandatory" src/components/kanban/kanban-board.tsx src/components/pipeline/pipeline-board.tsx src/components/potential-projects/potential-board.tsx`
</verification>

<success_criteria>
- All 3 Kanban boards compile and render
- TouchSensor configured with 250ms delay on all boards
- Board containers use overflow-x-auto with snap-x snap-mandatory
- Desktop behavior unchanged (md:min-w-max preserves current layout)
</success_criteria>

<output>
After completion, create `.planning/phases/17-kanban-responsive/17-01-SUMMARY.md`
</output>

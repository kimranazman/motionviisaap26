---
phase: 01-navigation-detail-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/sidebar.tsx
  - src/components/layout/header.tsx
  - src/components/initiatives/initiatives-list.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar has no Settings link (no 404 when clicking nav)"
    - "User dropdown shows only user name/email label (no non-functional items)"
    - "Initiative list shows only View button (no dead Edit link)"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Sidebar without Settings link"
      contains: "navigation = ["
      must_not_contain: "Settings"
    - path: "src/components/layout/header.tsx"
      provides: "Header with clean user dropdown"
      must_not_contain: "Profile"
    - path: "src/components/initiatives/initiatives-list.tsx"
      provides: "Initiatives table with working links"
      contains: "/initiatives/${initiative.id}"
      must_not_contain: "/edit"
  key_links:
    - from: "sidebar.tsx"
      to: "valid routes only"
      via: "navigation array"
      pattern: "href.*/(timeline|kanban|calendar|initiatives|events)"
---

<objective>
Remove dead navigation links that lead to 404 pages.

Purpose: Users currently hit dead ends clicking Settings in sidebar and Profile/Settings/Logout in user dropdown. The Edit link on initiatives leads to non-existent `/initiatives/[id]/edit` route.

Output: Clean navigation with only functional links. Users navigate without hitting 404s.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-navigation-detail-page/01-RESEARCH.md

@src/components/layout/sidebar.tsx
@src/components/layout/header.tsx
@src/components/initiatives/initiatives-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Settings link from sidebar</name>
  <files>src/components/layout/sidebar.tsx</files>
  <action>
    1. Remove the Settings icon import from lucide-react (line 12: `Settings`)
    2. Remove the entire Footer section (lines 65-83) that contains the Settings link
    3. The revenue target info in the footer can be removed too - it's static display only

    Keep: All navigation items in the `navigation` array (Dashboard, Timeline, Kanban, Calendar, Initiatives, Events to Attend)

    Result: Sidebar shows only the logo and main navigation, no footer.
  </action>
  <verify>
    Run `npm run build` - no TypeScript errors about unused Settings import.
    Grep for "Settings" in sidebar.tsx - should return nothing.
  </verify>
  <done>Sidebar has no Settings link. Footer section removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up user dropdown in header</name>
  <files>src/components/layout/header.tsx</files>
  <action>
    1. Keep the DropdownMenuLabel showing user name and email (lines 61-68)
    2. Remove line 69 (DropdownMenuSeparator after label)
    3. Remove lines 70-73 (Profile, Settings, Separator, Log out menu items)

    The dropdown should ONLY show:
    - User name (Khairul)
    - Email (khairul@motionvii.com)

    Why: Profile, Settings, and Logout require auth system which is out of scope (v2).

    Result: Clicking avatar shows info-only dropdown with no clickable items.
  </action>
  <verify>
    Run `npm run build` - no errors.
    Grep for "Profile" in header.tsx - should return nothing.
    Grep for "Log out" in header.tsx - should return nothing.
  </verify>
  <done>User dropdown shows only name/email label, no menu items.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Edit link from initiatives list</name>
  <files>src/components/initiatives/initiatives-list.tsx</files>
  <action>
    1. Remove the Pencil icon import from lucide-react (line 41: `Pencil`)
    2. Remove lines 213-217: the Edit button that links to `/initiatives/${initiative.id}/edit`
    3. Keep the View button (Eye icon) that links to `/initiatives/${initiative.id}`

    The Actions column should show only the View (Eye) button.

    Why: Edit will happen inline on the detail page (Plan 02), not on a separate edit route.
  </action>
  <verify>
    Run `npm run build` - no TypeScript errors about unused Pencil import.
    Grep for "/edit" in initiatives-list.tsx - should return nothing.
    Grep for "Pencil" in initiatives-list.tsx - should return nothing.
  </verify>
  <done>Initiatives list shows only View button in Actions column.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npm run lint` passes with no warnings
3. Manual check: Sidebar shows no Settings link
4. Manual check: User dropdown shows only name/email
5. Manual check: Initiatives table shows only View button
</verification>

<success_criteria>
- Build succeeds without errors
- No "Settings" in sidebar.tsx
- No "Profile", "Settings", "Log out" in header.tsx dropdown items
- No "/edit" route references in initiatives-list.tsx
- No unused imports (Pencil, Settings)
</success_criteria>

<output>
After completion, create `.planning/phases/01-navigation-detail-page/01-01-SUMMARY.md`
</output>

# Plan 76-01: Project Kanban Column Component

```yaml
wave: 1
depends_on: []
files_modified:
  - src/components/projects/project-kanban-column.tsx (NEW)
autonomous: true
```

## Objective

Create the droppable column component for the Projects Kanban board.

## Tasks

<task id="1">
Create `src/components/projects/project-kanban-column.tsx` following the pattern from `src/components/tasks/task-kanban-column.tsx`:
- Import `useDroppable` from `@dnd-kit/core`
- Props: id, title, colorDot, count, children
- Use `setNodeRef` from `useDroppable({ id })`
- Apply `isOver` styling with ring highlight
- Mobile-responsive widths: `w-[75vw] min-w-[280px] max-w-[320px]` mobile, `md:w-80` desktop
- Header with color dot, title, count badge
- Cards container with `min-h-[200px]`
- Apply `snap-start` for horizontal scroll snap
</task>

## Verification

```bash
# File exists
ls src/components/projects/project-kanban-column.tsx

# Has correct imports
grep "useDroppable" src/components/projects/project-kanban-column.tsx
grep "@dnd-kit/core" src/components/projects/project-kanban-column.tsx

# Exports component
grep "export function ProjectKanbanColumn" src/components/projects/project-kanban-column.tsx
```

## must_haves

- [ ] Component accepts id, title, colorDot, count, children props
- [ ] Uses useDroppable hook with column id
- [ ] Shows visual feedback (ring) when item dragged over
- [ ] Mobile-responsive column width
- [ ] Header displays color dot, title, and count badge
